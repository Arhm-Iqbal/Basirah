# Copy to .env.local in frontend/ and to .dev.vars in backend/, filling only the section
# that side needs. Never commit a filled-in copy.

# ---- frontend ----
# Anything prefixed NEXT_PUBLIC_ is shipped to the browser. Never put a secret behind it.
NEXT_PUBLIC_API_URL=http://localhost:8787
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Public identifier for the browser widget. Set it together with TURNSTILE_SECRET_KEY.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=

# ---- backend ----
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Powers report next-steps guidance and event extraction from mosque websites.
# Without it both degrade quietly rather than failing: guidance returns 503 and event
# ingestion falls back to ICS feeds and admin-entered events.
OPENAI_API_KEY=
# One key, restricted to Places API (New) + Fact Check Tools API. Has no application
# restriction because Workers egress from rotating IPs, so it must never reach the browser.
GOOGLE_API_KEY=
# Unset locally skips the bot check. Production must set it or the anonymous
# endpoints are open to automated submission.
TURNSTILE_SECRET_KEY=
ALLOWED_ORIGINS=http://localhost:3000
